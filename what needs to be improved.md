Todo:
- Code quality ko improvement (Amit)
- Conversation history management using something in langchain (Amit)
- Chunking algorithm is custom. is there anything in langchain we can use. Also do we continue or langchain? (Amit)
- Metadata filtering + semantic search (very good and working well.). We should actually test if our retrieval is good or not. and find cases where it's bad and then try to fix for them. Build -> Test -> fix -> Test -> production (Karina)
- top k = 6, number of subheadings at max 10. (Karina)
- attach the actual .md file and datasets in the answer. or imagine a new UI. -> Your reveiwers will like this. (Karina)
- citations - which chunks were retrieved from vectordb and show that. (Karina)
